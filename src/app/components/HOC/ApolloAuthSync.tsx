"use client";
/**
 * ApolloAuthSync stub for v99.
 * v99 uses internal Next.js API routes instead of Apollo/GraphQL.
 * This component is a no-op kept for compatibility.
 */
export default function ApolloAuthSync() {
  return null;
}
