# Archived Note

The nginx/VPS ingress path is no longer a supported production deployment for
this repository.

Supported production topology:

- Railway for the API
- Vercel for the client
- managed TLS on both platforms

Keep this directory only as historical reference. Do not use it as the active
production ingress configuration.
